diff --git a/common/arg.cpp b/common/arg.cpp
index 841ca3ce2..5e257614c 100644
--- a/common/arg.cpp
+++ b/common/arg.cpp
@@ -307,6 +307,7 @@ const std::vector<ggml_type> kv_cache_types = {
     GGML_TYPE_IQ4_NL,
     GGML_TYPE_Q5_0,
     GGML_TYPE_Q5_1,
+    GGML_TYPE_Q4_HQQ,
 };
 
 static ggml_type kv_cache_type_from_str(const std::string & s) {
@@ -2426,6 +2427,14 @@ common_params_context common_params_parser_init(common_params & params, llama_ex
             params.devices = parse_device_list(value);
         }
     ).set_env("LLAMA_ARG_DEVICE"));
+    add_opt(common_arg(
+        {"-mb", "--mmproj-backend"}, "<dev1,dev2,..>",
+        "specific backend for only the multimodal projector\n"
+        "use --list-devices to see a list of available devices",
+        [](common_params & params, const std::string & value) {
+            params.mmproj_devices = parse_device_list(value);
+        }
+    ).set_env("LLAMA_ARG_MMBACKEND"));
     add_opt(common_arg(
         {"--list-devices"},
         "print list of available devices and exit",
diff --git a/common/common.cpp b/common/common.cpp
index a14e7bbed..004bd68aa 100644
--- a/common/common.cpp
+++ b/common/common.cpp
@@ -1530,7 +1530,12 @@ void common_set_adapter_lora(struct llama_context * ctx, std::vector<common_adap
 struct llama_model_params common_model_params_to_llama(common_params & params) {
     auto mparams = llama_model_default_params();
 
-    if (!params.devices.empty()) {
+    const bool has_mmproj = !params.mmproj.path.empty();
+
+    if(has_mmproj && !params.mmproj_devices.empty()){
+        mparams.devices = params.mmproj_devices.data();
+    }
+    else if (!params.devices.empty()) {
         mparams.devices = params.devices.data();
     }
 
diff --git a/common/common.h b/common/common.h
index 94147d5d8..1766ef0ab 100644
--- a/common/common.h
+++ b/common/common.h
@@ -583,6 +583,8 @@ struct common_params {
 
     // multimodal models (see tools/mtmd)
     struct common_params_model mmproj;
+    std::vector<ggml_backend_dev_t> mmproj_devices; // devices to use for offloading
+
     bool mmproj_use_gpu = true;     // use GPU for multimodal model
     bool no_mmproj = false;         // explicitly disable multimodal model
     std::vector<std::string> image; // path to image file(s) ; TODO: change the name to "media"
diff --git a/tools/server/server-context.cpp b/tools/server/server-context.cpp
index 5c33a418f..b85de42f4 100644
--- a/tools/server/server-context.cpp
+++ b/tools/server/server-context.cpp
@@ -1121,7 +1121,11 @@ private:
                     GGML_ASSERT(!params_base.fit_params_target.empty());
                     size_t total = 0;
 
-                    std::vector<ggml_backend_dev_t> tgt_devices = params.devices;
+                    std::vector<ggml_backend_dev_t> tgt_devices;
+                    if (has_mmproj && !params.mmproj_devices.empty())
+                        tgt_devices = params.mmproj_devices;
+                    else
+                        tgt_devices = params.devices;
 
                     if (tgt_devices.empty()) {
                         for(size_t i = 0; i < ggml_backend_dev_count(); ++i) {
